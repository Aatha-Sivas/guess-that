import { ExpoConfig } from 'expo/config';
const config: ExpoConfig = {
    name: 'Guess That',
    slug: 'guess-that',
    scheme: 'guessthat',
    android: {
        package: "com.guessthat",
    },
    extra: {}
};
export default config;
